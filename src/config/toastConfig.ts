/**
 * Toast duration configuration.
 * Override specific values as needed — all durations are in milliseconds.
 */
export const toastConfig = {
  duration: {
    success: 2500,
    error: 4000,
    info: 3000,
    warning: 3500,
  },
} as const;
